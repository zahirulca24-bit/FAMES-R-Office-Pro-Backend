[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -u scripts/run_migrations.py && exec uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health/ready"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
